version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: videography-tool-web
    ports:
      - "8080:80"
    networks:
      - default
      - tools_network
    restart: always

networks:
  tools_network:
    external: true


